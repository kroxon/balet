import '../styles.css';

document.addEventListener('DOMContentLoaded', () => {
    console.log('DOM fully loaded and parsed');
    // zmienSekcje('home');  

    // initializeGaleria();
    // initializeBlog();
    // applyAnimations();
});