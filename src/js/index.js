import '../styles.css';

document.addEventListener('DOMContentLoaded', () => {
    console.log('DOM fully loaded and parsed');
    // zmienSekcje  

    // initializeGaleria();
    // initializeBlog();
    // applyAnimations();
});

